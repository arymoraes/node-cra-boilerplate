import React, { ReactElement } from 'react'

interface Props {
    
}

export default function addGame({}: Props): ReactElement {
    return (
        <div>
            Boi Li Max
        </div>
    )
}
